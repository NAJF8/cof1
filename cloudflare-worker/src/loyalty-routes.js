import * as security from './loyalty-security.js';
import { firebaseAdminRequest, firebaseAdminAtomicPatch } from './firebase-admin.js';
import { planGiftDecision, planGiftRedemption } from './gift-delivery.js';

const PROJECT_ID = 'coffee-30fa7';
const SUPER_ADMIN_EMAIL = 'mohameadalhaear100@gmail.com';
const MAX_BODY = 16 * 1024;
const MAX_PRODUCT_IMAGE_BYTES = 5 * 1024 * 1024;
const PRODUCT_IMAGE_TYPES = new Set(['image/jpeg', 'image/png', 'image/webp']);
const PRODUCT_IMAGE_RATE_LIMIT = 30;
const PRODUCT_IMAGE_RATE_WINDOW_MS = 10 * 60 * 1000;
const productImageRateBuckets = new Map();
let publicKeys = { expiresAt: 0, value: {} };
let customTokenKey = { fingerprint: '', value: null };

function origins(env) { return String(env.ALLOWED_ORIGINS || 'https://najf8.github.io').split(',').map(x => x.trim()).filter(Boolean); }
function cors(request, env) { const origin = request.headers.get('Origin'); return origin && origins(env).includes(origin) ? { 'Access-Control-Allow-Origin': origin, 'Access-Control-Allow-Methods': 'GET, POST, OPTIONS', 'Access-Control-Allow-Headers': 'Content-Type, Authorization', 'Cache-Control': 'no-store', Vary: 'Origin' } : null; }
function response(request, env, body, status = 200) { return new Response(JSON.stringify(body), { status, headers: { 'Content-Type': 'application/json; charset=utf-8', 'Cache-Control': 'no-store', 'X-Content-Type-Options': 'nosniff', ...(cors(request, env) || {}) } }); }
function fail(request, env, error, status) { return response(request, env, { ok: false, error }, status); }
async function body(request) { const type = request.headers.get('Content-Type') || ''; if (!type.toLowerCase().includes('application/json')) throw Error('INVALID_CONTENT_TYPE'); const raw = await request.text(); if (new TextEncoder().encode(raw).byteLength > MAX_BODY) throw Error('PAYLOAD_TOO_LARGE'); const value = JSON.parse(raw); if (!value || typeof value !== 'object' || Array.isArray(value)) throw Error('INVALID_JSON'); return value; }
function b64(value) { let text = String(value || '').replace(/-/g, '+').replace(/_/g, '/'); while (text.length % 4) text += '='; return Uint8Array.from(atob(text), c => c.charCodeAt(0)); }
function b64url(bytes) { let text = ''; for (const byte of new Uint8Array(bytes)) text += String.fromCharCode(byte); return btoa(text).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/g, ''); }
function jsonPart(value) { return b64url(new TextEncoder().encode(JSON.stringify(value))); }
function pemBytes(value) { return b64(String(value).replace(/-----BEGIN PRIVATE KEY-----|-----END PRIVATE KEY-----|\s+/g, '')); }
async function signingKeys() { if (publicKeys.expiresAt > Date.now() && Object.keys(publicKeys.value).length) return publicKeys.value; const r = await fetch('https://www.googleapis.com/service_accounts/v1/jwk/securetoken@system.gserviceaccount.com'); if (!r.ok) throw Error('AUTH_KEYS_UNAVAILABLE'); const value = {}; for (const key of (await r.json()).keys || []) if (key.kid) value[key.kid] = key; publicKeys = { value, expiresAt: Date.now() + 300000 }; return value; }
async function verifyIdToken(token) { const parts = String(token || '').split('.'); if (parts.length !== 3) throw Error('AUTH_INVALID'); let header, claims; try { header = JSON.parse(new TextDecoder().decode(b64(parts[0]))); claims = JSON.parse(new TextDecoder().decode(b64(parts[1]))); } catch { throw Error('AUTH_INVALID'); } const jwk = (await signingKeys())[header.kid]; if (header.alg !== 'RS256' || !jwk) throw Error('AUTH_INVALID'); const key = await crypto.subtle.importKey('jwk', jwk, { name: 'RSASSA-PKCS1-v1_5', hash: 'SHA-256' }, false, ['verify']); if (!await crypto.subtle.verify('RSASSA-PKCS1-v1_5', key, b64(parts[2]), new TextEncoder().encode(`${parts[0]}.${parts[1]}`))) throw Error('AUTH_INVALID'); const now = Math.floor(Date.now() / 1000); if (!claims.sub || claims.aud !== PROJECT_ID || claims.iss !== `https://securetoken.google.com/${PROJECT_ID}` || Number(claims.exp) <= now || Number(claims.iat || 0) > now + 300) throw Error('AUTH_INVALID'); return { uid: String(claims.sub), email: String(claims.email || '').toLowerCase(), emailVerified: claims.email_verified === true, name: String(claims.name || ''), picture: String(claims.picture || '') }; }
async function auth(request) { const header = request.headers.get('Authorization') || ''; if (!header.startsWith('Bearer ')) throw Error('AUTH_REQUIRED'); return verifyIdToken(header.slice(7).trim()); }
async function customToken(env, uid) { const email = String(env.FIREBASE_SERVICE_ACCOUNT_EMAIL || '').trim(), privateKey = String(env.FIREBASE_SERVICE_ACCOUNT_PRIVATE_KEY || ''); if (!email || !privateKey) throw Error('FIREBASE_SERVICE_ACCOUNT_NOT_CONFIGURED'); const fingerprint = privateKey.slice(0, 24); if (customTokenKey.fingerprint !== fingerprint) customTokenKey = { fingerprint, value: await crypto.subtle.importKey('pkcs8', pemBytes(privateKey), { name: 'RSASSA-PKCS1-v1_5', hash: 'SHA-256' }, false, ['sign']) }; const now = Math.floor(Date.now() / 1000), head = jsonPart({ alg: 'RS256', typ: 'JWT' }), claim = jsonPart({ iss: email, sub: uid, aud: 'https://identitytoolkit.googleapis.com/google.identity.identitytoolkit.v1.IdentityToolkit', iat: now, exp: now + 3600, claims: { loyaltyMembership: uid.replace('loyalty-member:', '') } }), signature = await crypto.subtle.sign('RSASSA-PKCS1-v1_5', customTokenKey.value, new TextEncoder().encode(`${head}.${claim}`)); return `${head}.${claim}.${b64url(signature)}`; }
function safeCustomer(membership, customer) { return security.publicProfile(membership, customer); }
function requestId(payload) { const value = String(payload?.requestId || payload?.idempotencyKey || '').trim(); if (!value) return ''; if (!/^[A-Za-z0-9._:-]{1,120}$/.test(value)) throw Error('INVALID_ARGUMENT'); return value; }
function operationPath(kind, request) { return request ? `loyalty_operation_requests/${kind}/${encodeURIComponent(request).replace(/%/g, '_')}` : ''; }
function replayOrPlan(root, kind, request) { if (!request) return null; const saved = root.loyalty_operation_requests?.[kind]?.[encodeURIComponent(request).replace(/%/g, '_')]; return saved?.result ? { replay: true, result: saved.result } : null; }
async function atomicPlan(env, plan) { return firebaseAdminAtomicPatch(env, plan, { attempts: 12 }); }
async function staff(env, current, capability = 'staff') { let record = {}; try { record = await firebaseAdminRequest(env, `admins/${current.uid}`) || {}; } catch (error) { if (!(current.emailVerified && current.email === SUPER_ADMIN_EMAIL)) throw error; } const allowlisted = current.emailVerified && current.email === SUPER_ADMIN_EMAIL; const role = String(record.role || (allowlisted ? 'super_admin' : '')).trim().toLowerCase(); const bootstrap = capability === 'provision-super-admin' && allowlisted; if (record.status !== 'active' && !allowlisted && !bootstrap) throw Error('FORBIDDEN'); const permissions = record.permissions || {}; const full = ['super_admin', 'admin', 'manager'].includes(role); const cashier = role === 'cashier' && ['search', 'adjust', 'redeem', 'consume', 'redeem-gift', 'manage-gifts'].includes(capability); const permitted = full || cashier || permissions[capability] === true || bootstrap; if (!permitted || (capability === 'delete' || capability === 'provision-super-admin') && role !== 'super_admin' && !bootstrap) throw Error('FORBIDDEN'); if (bootstrap) await firebaseAdminRequest(env, `admins/${current.uid}`, { method: 'PATCH', body: { email: current.email, role: 'super_admin', status: 'active', displayName: current.name || 'Super Admin', addedBy: 'server', addedAt: Date.now() } }); return { record, role }; }
function productImageType(bytes) { if (bytes.length >= 3 && bytes[0] === 0xff && bytes[1] === 0xd8 && bytes[2] === 0xff) return 'image/jpeg'; if (bytes.length >= 8 && bytes.slice(0, 8).every((value, index) => value === [137, 80, 78, 71, 13, 10, 26, 10][index])) return 'image/png'; if (bytes.length >= 12 && String.fromCharCode(...bytes.slice(0, 4)) === 'RIFF' && String.fromCharCode(...bytes.slice(8, 12)) === 'WEBP') return 'image/webp'; return ''; }
function consumeProductImageRate(uid) { const now = Date.now(), current = productImageRateBuckets.get(uid); if (!current || now >= current.resetAt) { productImageRateBuckets.set(uid, { count: 1, resetAt: now + PRODUCT_IMAGE_RATE_WINDOW_MS }); return true; } if (current.count >= PRODUCT_IMAGE_RATE_LIMIT) return false; current.count += 1; return true; }
function base64Bytes(bytes) { let output = ''; for (let offset = 0; offset < bytes.length; offset += 0x8000) output += String.fromCharCode(...bytes.slice(offset, offset + 0x8000)); return btoa(output); }
async function uploadProductImage(request, env, current) { await staff(env, current, 'manage-products'); if (!consumeProductImageRate(current.uid)) throw Error('IMAGE_RATE_LIMITED'); const file = (await request.formData()).get('image'); if (!file || typeof file.arrayBuffer !== 'function') throw Error('IMAGE_REQUIRED'); if (Number(file.size) > MAX_PRODUCT_IMAGE_BYTES) throw Error('IMAGE_TOO_LARGE'); const bytes = new Uint8Array(await file.arrayBuffer()), type = String(file.type || '').toLowerCase(); if (!PRODUCT_IMAGE_TYPES.has(type) || productImageType(bytes) !== type) throw Error('IMAGE_TYPE_UNSUPPORTED'); const token = String(env.GITHUB_PRODUCT_IMAGES_TOKEN || '').trim(); if (!token) throw Error('IMAGE_STORAGE_NOT_CONFIGURED'); const extension = type === 'image/jpeg' ? 'jpg' : type.slice(6), filename = `${Date.now()}-${crypto.randomUUID().replace(/-/g, '')}.${extension}`, path = `assets/products/${filename}`; const github = await fetch(`https://api.github.com/repos/NAJF8/cof1/contents/${path}`, { method: 'PUT', headers: { Accept: 'application/vnd.github+json', Authorization: `Bearer ${token}`, 'X-GitHub-Api-Version': '2022-11-28', 'User-Agent': '101-coffee-product-image-uploader', 'Content-Type': 'application/json' }, body: JSON.stringify({ message: 'chore: upload product image', content: base64Bytes(bytes), branch: 'main' }) }); if (!github.ok) throw Error('IMAGE_UPLOAD_FAILED'); return response(request, env, { success: true, imageUrl: `https://101coffees.com/assets/products/${encodeURIComponent(filename)}`, path }); }
async function login(request, env) { const payload = await body(request), membership = security.normalizeMembershipNumber(payload.membershipNumber), pin = String(payload.pin || ''), pepper = String(env.LOYALTY_PIN_PEPPER || ''); if (!membership || !security.validPin(pin) || !pepper) return fail(request, env, 'INVALID_CREDENTIALS', 401); const key = await security.attemptKey(membership, request.headers.get('CF-Connecting-IP') || 'unknown', pepper), state = await firebaseAdminRequest(env, `loyalty_login_attempts/${key}`) || {}, now = Date.now(); if (Number(state.lockedUntil) > now || Number(state.failedAttempts) >= security.MAX_FAILURES && now - Number(state.firstFailureAt || 0) < security.WINDOW_MS) return fail(request, env, 'RATE_LIMITED', 429); const [customer, credential] = await Promise.all([firebaseAdminRequest(env, `loyalty_customers/${membership}`), firebaseAdminRequest(env, `loyalty_credentials/${membership}`)]); let valid = await security.timingSafePinMatch(pin, credential, pepper); if (!valid && !credential && customer && security.validPin(customer.pin)) { valid = security.timingSafeEqual(new TextEncoder().encode(pin), new TextEncoder().encode(String(customer.pin))); if (valid) await firebaseAdminRequest(env, `loyalty_credentials/${membership}`, { method: 'PUT', body: await security.createCredential(pin, pepper, now) }); } if (!customer || !valid) { const within = Number(state.firstFailureAt) > 0 && now - Number(state.firstFailureAt) < security.WINDOW_MS, failures = within ? Number(state.failedAttempts || 0) + 1 : 1; await firebaseAdminRequest(env, `loyalty_login_attempts/${key}`, { method: 'PUT', body: { failedAttempts: failures, firstFailureAt: within ? Number(state.firstFailureAt) : now, lastFailureAt: now, lockedUntil: failures >= security.MAX_FAILURES ? now + security.WINDOW_MS : 0 } }); return fail(request, env, failures >= security.MAX_FAILURES ? 'RATE_LIMITED' : 'INVALID_CREDENTIALS', failures >= security.MAX_FAILURES ? 429 : 401); } await firebaseAdminRequest(env, `loyalty_login_attempts/${key}`, { method: 'DELETE' }); return response(request, env, { ok: true, token: await customToken(env, `loyalty-member:${membership}`), profile: safeCustomer(membership, customer) }); }
async function profile(request, env, current) { const membership = security.normalizeMembershipNumber(await firebaseAdminRequest(env, `loyalty_links/${current.uid}`)); if (!membership) return fail(request, env, 'PROFILE_NOT_FOUND', 404); const customer = await firebaseAdminRequest(env, `loyalty_customers/${membership}`); if (!customer || String(customer.uid || '') !== current.uid) return fail(request, env, 'FORBIDDEN', 403); return response(request, env, { ok: true, profile: safeCustomer(membership, customer) }); }
async function provision(request, env, current, superAdmin = false) {
  if (!current.emailVerified || !current.email) return fail(request, env, 'VERIFIED_EMAIL_REQUIRED', 403);
  if (superAdmin && current.email !== SUPER_ADMIN_EMAIL) throw Error('FORBIDDEN');
  const linked = security.normalizeMembershipNumber(await firebaseAdminRequest(env, `loyalty_links/${current.uid}`));
  const pending = await firebaseAdminRequest(env, `loyalty_pending/${current.uid}`) || {};
  const counter = Number(await firebaseAdminRequest(env, 'loyalty_counter')) || 0;
  const membership = linked || (superAdmin ? '101-1' : `101-${counter + 1}`);
  const existing = await firebaseAdminRequest(env, `loyalty_customers/${membership}`) || {};
  const customer = { ...existing, uid: current.uid, email: current.email, name: String(existing.name || pending.displayName || current.name || 'عضو 101').slice(0, 120), memberType: superAdmin ? 'Super Admin' : existing.memberType || 'زبون', hearts: Number(existing.hearts || 0), currentHearts: Number(existing.currentHearts ?? existing.hearts ?? 0), updatedAt: Date.now() };
  const updates = { [`loyalty_customers/${membership}`]: customer, [`loyalty_links/${current.uid}`]: membership, loyalty_counter: Math.max(Number(membership.split('-')[1]), counter) };
  if (pending.status === 'pending') updates[`loyalty_pending/${current.uid}`] = null;
  await firebaseAdminRequest(env, '', { method: 'PATCH', body: updates });
  return response(request, env, { ok: true, provisioned: !linked, membershipNumber: membership, profile: safeCustomer(membership, customer) });
}
async function activatePending(request, env, current) { await staff(env, current, 'activate-pending'); const p = await body(request), uid = String(p.uid || '').trim(), pending = await firebaseAdminRequest(env, `loyalty_pending/${uid}`); if (!/^[A-Za-z0-9_-]{6,180}$/.test(uid) || !pending || pending.status !== 'pending') throw Error('INVALID_PENDING'); const existing = await firebaseAdminRequest(env, `loyalty_links/${uid}`); if (existing) { await firebaseAdminRequest(env, `loyalty_pending/${uid}`, { method: 'DELETE' }); return response(request, env, { ok: true, membershipNumber: existing, existing: true }); } const counter = (Number(await firebaseAdminRequest(env, 'loyalty_counter')) || 0) + 1, membership = `101-${counter}`, pin = String(Math.floor(Math.random() * 10000)).padStart(4, '0'), now = Date.now(); await firebaseAdminRequest(env, '', { method: 'PATCH', body: { [`loyalty_customers/${membership}`]: { uid, name: String(pending.displayName || pending.email || 'عضو 101').slice(0, 120), email: String(pending.email || '').slice(0, 180), memberType: 'زبون', membershipStatus: 'عضو مميز', hearts: 0, currentHearts: 0, pin, createdAt: now, updatedAt: now }, [`loyalty_links/${uid}`]: membership, [`loyalty_pending/${uid}`]: null, loyalty_counter: counter } }); return response(request, env, { ok: true, membershipNumber: membership, existing: false }); }
async function changeMembership(request, env, current) { await staff(env, current, 'change-membership'); const p = await body(request), oldId = security.normalizeMembershipNumber(p.oldId), newId = security.normalizeMembershipNumber(p.newId), id = requestId(p); if (!oldId || !newId || oldId === newId) throw Error('INVALID_MEMBERSHIP'); const result = await atomicPlan(env, (root) => { const replay = replayOrPlan(root, 'membership-change', id); if (replay) return replay; const customer = root.loyalty_customers?.[oldId]; if (!customer) throw Error('NOT_FOUND'); if (root.loyalty_customers?.[newId]) throw Error('ALREADY_EXISTS'); const outcome = { ok: true, membershipNumber: newId }; const updates = { [`loyalty_customers/${newId}`]: customer, [`loyalty_customers/${oldId}`]: null, ...(customer.uid ? { [`loyalty_links/${customer.uid}`]: newId } : {}) }; if (id) updates[operationPath('membership-change', id)] = { result: outcome, createdAt: Date.now() }; return { updates, result: outcome }; }); return response(request, env, result); }
async function search(request, env, current) { await staff(env, current, 'search'); const p = await body(request), q = String(p.query || '').trim().toLowerCase(), all = await firebaseAdminRequest(env, 'loyalty_customers') || {}; const found = Object.entries(all).map(([membership, customer]) => ({ membership, customer })).find(({ membership, customer }) => !q || [membership, customer.name, customer.email, customer.phone].some(v => String(v || '').toLowerCase().includes(q))); return response(request, env, { ok: true, customer: found ? safeCustomer(found.membership, found.customer) : null }); }
async function deleteCustomer(request, env, current) { await staff(env, current, 'delete'); const p = await body(request), membership = security.normalizeMembershipNumber(p.membership); if (!membership) throw Error('INVALID_MEMBERSHIP'); const customer = await firebaseAdminRequest(env, `loyalty_customers/${membership}`); if (!customer) return response(request, env, { ok: true, deleted: false }); await firebaseAdminRequest(env, '', { method: 'PATCH', body: { [`loyalty_customers/${membership}`]: null, ...(customer.uid ? { [`loyalty_links/${customer.uid}`]: null } : {}) } }); return response(request, env, { ok: true, deleted: true }); }
async function adjustHearts(request, env, current) { await staff(env, current, 'adjust'); const p = await body(request), membership = security.normalizeMembershipNumber(p.membership || p.customerId), delta = Number(p.amount ?? p.hearts ?? p.change); if (!membership || !Number.isInteger(delta) || Math.abs(delta) > 1000) throw Error('INVALID_ARGUMENT'); const result = await atomicPlan(env, (root) => { const customer = root.loyalty_customers?.[membership]; if (!customer) throw Error('NOT_FOUND'); const currentHearts = Number(customer.currentHearts ?? customer.hearts ?? 0), next = currentHearts + delta; if (!Number.isInteger(currentHearts) || next < 0 || next > 5) throw Error('HEARTS_OUT_OF_RANGE'); return { updates: { [`loyalty_customers/${membership}/currentHearts`]: next, [`loyalty_customers/${membership}/hearts`]: next, [`loyalty_customers/${membership}/updatedAt`]: Date.now() }, result: { ok: true, profile: safeCustomer(membership, { ...customer, currentHearts: next, hearts: next }) } }; }); return response(request, env, result); }
async function redeem(request, env, current) { await staff(env, current, 'redeem'); const p = await body(request), membership = security.normalizeMembershipNumber(p.membership || p.customerId), required = Number(p.requiredHearts ?? p.cost ?? 0), id = requestId(p); if (!membership || !Number.isInteger(required) || required <= 0) throw Error('INVALID_ARGUMENT'); const result = await atomicPlan(env, (root) => { const replay = replayOrPlan(root, 'redeem', id); if (replay) return replay; const customer = root.loyalty_customers?.[membership], hearts = Number(customer?.currentHearts ?? customer?.hearts ?? 0); if (!customer || hearts < required) throw Error('INSUFFICIENT_HEARTS'); const next = hearts - required, logId = id || crypto.randomUUID(), outcome = { ok: true, profile: safeCustomer(membership, { ...customer, currentHearts: next, hearts: next, totalHeartsSpent: Number(customer.totalHeartsSpent || 0) + required, totalHeartsRedeemed: Number(customer.totalHeartsRedeemed || 0) + required }) }; const updates = { [`loyalty_customers/${membership}/currentHearts`]: next, [`loyalty_customers/${membership}/hearts`]: next, [`loyalty_customers/${membership}/totalHeartsSpent`]: Number(customer.totalHeartsSpent || 0) + required, [`loyalty_customers/${membership}/totalHeartsRedeemed`]: Number(customer.totalHeartsRedeemed || 0) + required, [`loyalty_customers/${membership}/updatedAt`]: Date.now(), [`loyalty_redemption_logs/${logId}`]: { membership, requiredHearts: required, createdAt: Date.now(), requestId: id || null } }; if (id) updates[operationPath('redeem', id)] = { result: outcome, createdAt: Date.now() }; return { updates, result: outcome }; }); return response(request, env, result); }
async function redeemGift(request, env, current) { const actor = await staff(env, current, 'redeem-gift'); const lookup = await body(request); const allowed = ['giftId', 'giftCode', 'orderCode', 'recipientPhone']; if (!allowed.some(field => String(lookup[field] || '').trim())) throw Error('INVALID_ARGUMENT'); const result = await atomicPlan(env, root => planGiftRedemption(root, lookup, { uid: current.uid, name: actor.record.displayName || current.name, email: current.email, role: actor.role })); return response(request, env, result); }
async function listGiftOrders(request, env, current) { await staff(env, current, 'manage-gifts'); const root = await firebaseAdminRequest(env, 'gift_orders') || {}; const now = Date.now(); const orders = Object.entries(root).map(([id, value]) => { const gift = value || {}, expired = gift.giftStatus === 'active' && gift.expiresAt && Number(gift.expiresAt) <= now; const approvalStatus = gift.paymentStatus === 'rejected' || gift.giftStatus === 'cancelled' ? 'rejected' : gift.giftStatus === 'redeemed' ? 'redeemed' : gift.giftStatus === 'expired' || expired ? 'expired' : gift.paymentStatus === 'paid' || gift.giftStatus === 'active' ? 'approved' : 'pending'; return { id, orderCode: gift.orderCode || '', giftCode: gift.giftCode || '', senderName: gift.senderName || '', recipientName: gift.recipientName || '', recipientPhone: gift.recipientPhone || '', senderPhone: gift.senderPhone || '', productName: gift.productName || '', giftType: gift.giftType || '', giftValue: Number(gift.giftValue || 0), message: gift.message || '', createdAt: gift.createdAt || null, expiresAt: gift.expiresAt || null, paymentStatus: gift.paymentStatus || '', giftStatus: gift.giftStatus || '', approvalStatus, approvedAt: gift.approvedAt || null, rejectedAt: gift.rejectedAt || null, redeemedAt: gift.redeemedAt || null }; }).sort((a, b) => Number(b.createdAt || 0) - Number(a.createdAt || 0)); return response(request, env, { ok: true, orders }); }
async function decideGift(request, env, current, decision) { const actor = await staff(env, current, 'manage-gifts'); const payload = await body(request), id = String(payload.giftId || payload.id || '').trim(); if (!id) throw Error('INVALID_ARGUMENT'); const result = await atomicPlan(env, root => planGiftDecision(root, id, decision, { uid: current.uid, name: actor.record.displayName || current.name, email: current.email, role: actor.role })); return response(request, env, result); }
async function reservePin(request, env, current) { await staff(env, current, 'reserve-pin'); const p = await body(request), customerId = String(p.customerId || '').trim(); if (!customerId || customerId.length > 180) throw Error('INVALID_ARGUMENT'); for (let i = 0; i < 100; i++) { const pin = String(Math.floor(1000 + Math.random() * 9000)); try { const result = await atomicPlan(env, (root) => { if (root.subscription_pin_index?.[pin]) return null; return { updates: { [`subscription_pin_index/${pin}`]: customerId }, result: { ok: true, pin } }; }); if (result) return response(request, env, result); } catch (error) { if (error?.message !== 'FIREBASE_ATOMIC_PLAN_INVALID') throw error; } } throw Error('PIN_RESERVATION_FAILED'); }
async function consume(request, env, current) { await staff(env, current, 'consume'); const p = await body(request), id = String(p.subscriptionId || p.id || '').trim(), requestKey = requestId(p); if (!id) throw Error('INVALID_ARGUMENT'); const result = await atomicPlan(env, (root) => { const replay = replayOrPlan(root, 'consume', requestKey); if (replay) return replay; const sub = root.subscriptions?.[id], now = Date.now(); if (!sub || sub.status !== 'active' || Number(sub.expiresAt) <= now || Number(sub.remainingUses) <= 0) throw Error('CLUB_UNAVAILABLE'); const remaining = Number(sub.remainingUses) - 1, outcome = { ok: true, remainingUses: remaining }, updates = { [`subscriptions/${id}/remainingUses`]: remaining, [`subscriptions/${id}/updatedAt`]: now, [`club_consumption_logs/${id}_${requestKey || crypto.randomUUID()}`]: { subscriptionId: id, createdAt: now, requestId: requestKey || null } }; if (requestKey) updates[operationPath('consume', requestKey)] = { result: outcome, createdAt: now }; return { updates, result: outcome }; }); return response(request, env, result); }
async function submitClaim(request, env, current) { if (!current.emailVerified || !current.email) throw Error('VERIFIED_EMAIL_REQUIRED'); const p = await body(request), clubNumber = String(p.clubNumber || '').trim().toUpperCase(), pin = String(p.pin || '').trim(); if (!/^CLUB-101-\d+$/.test(clubNumber) || !/^\d{4,8}$/.test(pin)) throw Error('INVALID_ARGUMENT'); await firebaseAdminRequest(env, `subscription_account_claims/${current.uid}`, { method: 'PUT', body: { uid: current.uid, clubNumber, pin, displayName: current.name, email: current.email, status: 'pending', createdAt: Date.now() } }); return response(request, env, { ok: true, submitted: true }); }
async function approveClaim(request, env, current) { await staff(env, current, 'approve-claim'); const p = await body(request), uid = String(p.uid || p.claimId || '').trim(), claim = await firebaseAdminRequest(env, `subscription_account_claims/${uid}`), customers = await firebaseAdminRequest(env, 'subscription_customers') || {}; const entry = Object.entries(customers).find(([, c]) => String(c.clubNumber || '').toUpperCase() === String(claim?.clubNumber || '').toUpperCase() && String(c.pin || '') === String(claim?.pin || '')); if (!claim || claim.status !== 'pending' || !entry) throw Error('CLAIM_INVALID'); const [customerId, customer] = entry; const updates = { [`subscription_customers/${customerId}/uid`]: uid, [`subscription_customers/${customerId}/email`]: claim.email || customer.email || '', [`subscription_account_index/${uid}`]: customerId, [`subscription_account_claims/${uid}/status`]: 'approved', [`subscription_account_claims/${uid}/approvedAt`]: Date.now() }; if (customer.activeSubscriptionId) updates[`subscriptions/${customer.activeSubscriptionId}/uid`] = uid; await firebaseAdminRequest(env, '', { method: 'PATCH', body: updates }); return response(request, env, { ok: true, approved: true }); }
async function setPin(request, env, current) { await staff(env, current, 'set-pin'); const p = await body(request), id = String(p.customerId || p.id || '').trim(), pin = String(p.pin || '').trim(); if (!id || !/^\d{4,8}$/.test(pin)) throw Error('INVALID_ARGUMENT'); await firebaseAdminRequest(env, `subscription_customers/${id}`, { method: 'PATCH', body: { pin, updatedAt: Date.now() } }); return response(request, env, { ok: true, saved: true }); }
async function route(request, env, url) { if (!url.pathname.startsWith('/api/loyalty/') && !url.pathname.startsWith('/api/subscription/') && !url.pathname.startsWith('/api/admin/')) return null; if (request.method === 'OPTIONS') return cors(request, env) ? new Response(null, { status: 204, headers: cors(request, env) }) : fail(request, env, 'ORIGIN_NOT_ALLOWED', 403); if (!cors(request, env)) return fail(request, env, 'ORIGIN_NOT_ALLOWED', 403); try { if (url.pathname === '/api/loyalty/login' && request.method === 'POST') return await login(request, env); const current = await auth(request); const path = url.pathname; if (path === '/api/loyalty/profile' && ['GET', 'POST'].includes(request.method)) return await profile(request, env, current); if (path === '/api/loyalty/provision-google') return await provision(request, env, current); if (path === '/api/admin/provision-super-admin') { await staff(env, current, 'provision-super-admin'); return await provision(request, env, current, true); } if (path === '/api/subscription/claim') return await submitClaim(request, env, current); if (path === '/api/admin/loyalty/activate-pending') return await activatePending(request, env, current); if (path === '/api/admin/loyalty/change-membership') return await changeMembership(request, env, current); if (path === '/api/admin/loyalty/search') return await search(request, env, current); if (path === '/api/admin/loyalty/delete') return await deleteCustomer(request, env, current); if (path === '/api/admin/loyalty/adjust-hearts') return await adjustHearts(request, env, current); if (path === '/api/admin/loyalty/redeem') return await redeem(request, env, current); if (path === '/api/admin/gift/orders' && request.method === 'GET') return await listGiftOrders(request, env, current); if (path === '/api/admin/gift/approve' && request.method === 'POST') return await decideGift(request, env, current, 'approve'); if (path === '/api/admin/gift/reject' && request.method === 'POST') return await decideGift(request, env, current, 'reject'); if (path === '/api/admin/gift/redeem' && request.method === 'POST') return await redeemGift(request, env, current); if (path === '/api/admin/club/consume') return await consume(request, env, current); if (path === '/api/admin/club/reserve-pin') return await reservePin(request, env, current); if (path === '/api/admin/subscription/set-pin') return await setPin(request, env, current); if (path === '/api/admin/subscription/approve-claim') return await approveClaim(request, env, current); return fail(request, env, 'NOT_FOUND', 404); } catch (error) { const code = ['AUTH_REQUIRED', 'AUTH_INVALID', 'INVALID_CONTENT_TYPE', 'PAYLOAD_TOO_LARGE', 'FORBIDDEN', 'NOT_FOUND', 'ALREADY_EXISTS', 'GIFT_NOT_FOUND', 'GIFT_ALREADY_REDEEMED', 'GIFT_EXPIRED', 'GIFT_NOT_AVAILABLE', 'GIFT_ALREADY_DECIDED', 'GIFT_NOT_PENDING', 'INVALID_ARGUMENT', 'INVALID_MEMBERSHIP', 'INVALID_PENDING', 'INSUFFICIENT_HEARTS', 'HEARTS_OUT_OF_RANGE', 'CLUB_UNAVAILABLE', 'CLAIM_INVALID', 'PIN_RESERVATION_FAILED', 'VERIFIED_EMAIL_REQUIRED'].includes(error.message) ? error.message : 'REQUEST_FAILED'; const status = ['AUTH_REQUIRED', 'AUTH_INVALID'].includes(code) ? 401 : code === 'FORBIDDEN' ? 403 : code === 'GIFT_NOT_FOUND' ? 404 : code === 'NOT_FOUND' ? 404 : code === 'INVALID_CONTENT_TYPE' ? 415 : code === 'PAYLOAD_TOO_LARGE' ? 413 : 400; return fail(request, env, code, status); } }
async function handleProductImageRoute(request, env, url) {
  if (url.pathname !== '/api/admin/products/upload-image') return null;
  if (request.method === 'OPTIONS') return cors(request, env) ? new Response(null, { status: 204, headers: cors(request, env) }) : fail(request, env, 'ORIGIN_NOT_ALLOWED', 403);
  if (request.method !== 'POST') return fail(request, env, 'METHOD_NOT_ALLOWED', 405);
  if (!cors(request, env)) return fail(request, env, 'ORIGIN_NOT_ALLOWED', 403);
  try { return await uploadProductImage(request, env, await auth(request)); }
  catch (error) {
    const code = ['AUTH_REQUIRED', 'AUTH_INVALID', 'FORBIDDEN', 'IMAGE_REQUIRED', 'IMAGE_TYPE_UNSUPPORTED', 'IMAGE_TOO_LARGE', 'IMAGE_RATE_LIMITED', 'IMAGE_STORAGE_NOT_CONFIGURED', 'IMAGE_UPLOAD_FAILED'].includes(error?.message) ? error.message : 'IMAGE_UPLOAD_FAILED';
    const status = ['AUTH_REQUIRED', 'AUTH_INVALID'].includes(code) ? 401 : code === 'FORBIDDEN' ? 403 : code === 'IMAGE_TOO_LARGE' ? 413 : code === 'IMAGE_RATE_LIMITED' ? 429 : 400;
    return fail(request, env, code, status);
  }
}
async function handleLoyaltyRoutes(request, env, url) { return await handleProductImageRoute(request, env, url) || route(request, env, url); }
export { handleLoyaltyRoutes };
